[tox]
envlist =
    py33,
    py34,
    py35,

[testenv]
deps =
    codecov
    coverage
    pytest
commands =
    python setup.py --quiet clean develop
    coverage run -m pytest
    coverage combine --append
    coverage report -m
